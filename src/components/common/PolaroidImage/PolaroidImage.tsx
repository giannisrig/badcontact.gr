'use client'
import { StyledPolaroidImage } from '@/components/common/PolaroidImage/PolaroidImage.styles'

export const PolaroidImage = ({ children }) => {
  return <StyledPolaroidImage>{children}</StyledPolaroidImage>
}
